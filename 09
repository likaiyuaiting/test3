#include<stdio.h>
#include<stdlib.h>
//#include<string.h>
#define N 100
int strl(char* str)
{
	if (str == NULL)
		return 0;
	else {
		return 1 + strl(str + 1);
	}
}
int main(void)
{
	char str[N];
	gets(str);
	printf("字符串长度为：%d", strl(str));
	system("pause");
	return 0;
}
